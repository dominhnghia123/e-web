export enum roleUserEnum {
  admin = 'admin',
  user = 'user',
}

export enum paymentsEnum {
  online = 'online',
  offline = 'offline',
}

export enum statusEnum {
  pending = 'pending',
  processing = 'processing',
  done = 'done',
}
